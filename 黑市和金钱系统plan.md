# 江湖银两系统、悬浮黑市与实时拍卖行 最终方案

针对您的三条最新批注（增加拍卖模块、修改定价、狠狠削减获取数值），我重新梳理了整个经济系统的平衡与新模块的设计。以下是迭代后的完整细致大纲。

## 1. “银两”系统的底层控制与兼容
全局货币统称为“**银两**”。
- 所有的交易与奖励将严格挂钩在 `gameState.js` 的 `silver` 字段上。
- **老玩家兼容**：无论在何种情况下，只要读取到数据缺失 `silver`，将在后台及前端的 `login_success` 流程中安静地初始化为 `0`，不会产生报错。

## 2. 极其紧缩的“银两”获取细则 (数值重设)
为了凸显银两的购买力与物价，现在的银两获取将变得异常紧缩。一天肝满也需要好几天才能喝得起一杯咖啡。

*   **悬赏大厅 (`TaskHall`)**: 
    -   1~2星委托：不再必得，**只有 20% 概率** 赚取 `1 银两`。
    -   3星委托：保底赚取 `1 银两`。
    -   4星委托：保底赚取 `2 银两`。
    -   5星血印委托：保底赚取 `4 银两`。
*   **江湖奇遇 (`EncounterArena`)**:
    -   战胜第一个路人：不再给钱。
    -   战胜第二个高手：`1 银两`。
    -   战胜最终 Boss (打通奇遇)：`3 银两`。(全胜最高仅能拿到 4 银两)
*   **秘境寻宝 (`SecretRealm`)**:
    -   深度奖励极大幅度缩水：**每满 5 层**才计为 `1 银两`（例如打到13层只有 2 银两）。
    -   业力加成取消乘算，调整为小额定值：积德行善 `>5` 额外打赏 `1 银两`；杀人越货 `< -5` 额外掠夺 `2 银两`。

## 3. 「入黑市」悬浮图标与交互模态框
*   在主界面 (`MainMenu`) 右下角实现一个带有呼吸发光效果的悬浮按钮。
*   点击后呈现一个全局毛玻璃遮罩层（Modal），展示每日固定的 **5 件货品**。
*   **商品列阵**：
    -   **坑位一：【橙C美式】 (售价：29 银两)** <kbd>※ 按您要求上调</kbd>
        （消耗巨资后立即刷新今日的悬赏、奇遇、秘境次数凭证，回归零点状态）
    -   **坑位二 至 坑位五 (随机每日刷新)**：可能刷出价格亲民的残卷箱 (`8 银两`)、属性秘药 (`10 银两`)，或是极为昂贵的传说盲盒 (`50+ 银两`)。

## 4. 全新重磅模块：【玩家拍卖行】(实时 Socket 竞拍系统)
拍卖模块作为**黑市**的延伸部分，打破了原来仅靠系统产出的限制，打造真实的玩家互市 (P2P交易) 中心。

*   **入口**：在主界面 `MainMenu` 或黑市面板中新增「拍卖风云 / 玩家拍卖行」。
*   **上架与竞拍机制**：
    -   **自由定底价**：玩家（卖家）上架物品时自由设定“起拍银两（底价）”。
    -   **4小时结算倒计时**：每一单拍卖物品自上架起，开启 4 小时的全服竞拍倒计时。
    -   **实时 Socket 竞拍**：其他玩家实时看到倒计时与当前最高出价，可随时加价。倒计时归零时进行“定拍”，价高者得，系统通过对公聊天 (`logs`) 或者邮件发货，同时向卖家结算赚取的银两（可由系统抽取极少的手续费）。
*   **全服弹幕广播系统 (Broadcast System)**：
    -   **重金造势**：当任何玩家成功**上架**一件竞拍品，或者长达4小时的竞拍终于**一锤定音（有人拍到物品）**时，系统将通过 WebSocket 向所有在线玩家推送广播事件。
    -   **弹幕轮播特效**：在所有玩家的屏幕上方立刻以醒目的弹幕滚动形式播出通知，例如：“*【破劫公告】玩家 [绝世高手] 正在黑市上架罕见功法手抄本《降龙十八掌》（劣化版），起拍价：15银两！*”、“*【一锤定音】恭喜 [霸天] 以 45 银两的天价拍得宝具 [倚天剑]！*”。这种全服弹幕将极大增强游戏内的市井烟火气和参与感。
*   **允许上架的物品种类**：
    1.  **功法手抄本**：玩家可将自己已学会的功法（`SKILLS_DB`），以撰写“手抄本”的形式挂售（卖家不失去原有功法）。
        *   **【代际劣化惩罚】**：为了平衡传染式流通并保障“原典”的绝对价值，手抄本系统引入“多重劣化机制”。竞拍买家习得的功法为**“当前版本威力再降级”**的版本。
            - 如果卖家是从系统/奇遇掉落处学到的原典，买家学到的就是“劣化版”（威力/附带概率削减 30%）。
            - 如果卖家本身学的就是“劣化版”，他再转手写手抄本拍卖的话，下一个买家学到的就是“劣化版x2”（在原本削减 30% 的基础上再次大幅削弱）。越往后传就越残缺，以此切断无限低廉倒卖高阶功法的可能！
    2.  **罕世宝具 (Treasures)**：玩家可以将自己刷出或不再使用的宝具挂牌拍卖（挂售时暂扣，成交后彻底失去该宝具）。
    3.  **每日疲劳点数凭证**：极具创意的经济转换！“肝帝”和“氪佬”的阶级互通：玩家可以把自己当天**未使用的** 奇遇点数、秘境点数、每日任务次数直接打包作为点数凭证拍卖！买家拍得后，当天剩余的挑战使用配额将会直接增加。

---

## User Review Required

> [!IMPORTANT]
> - 现在的银币产出变得**非常稀少**，29 银两的橙C美式将是一个名副其实的“奢侈品”，肝帝需要稳定肝好几天才能喝一杯。请确认这样的紧缩数值是否足够“狠狠降”。
> - **实时的 Socket 拍卖行设计**是否符合您对“遗漏的拍卖模块”的构想？

如果您确认对数值和新加入的拍卖方案满意，请回复 **“通过”** 或者 **“完美”**。拿到您的绿灯，我将马上开始执行这套宏大的更新逻辑！
